{
  "name": "react-test",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel src/app.js --out-file=public/js/index.js --presets=react,env --watch",
    "server": "live-server public",
    "dev": "npm-run-all -p server babel"
  },
  "author": "Insaniti69",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@babel/polyfill": "^7.12.1",
    "axios": "^0.21.0"
  }
}
